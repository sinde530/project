import { render } from '@testing-library/react';

import App from './App';

const toggleDarkMode = () => {
  //
};

describe('<App/>', () => {
  it('renders App Component', () => {
    render(<App />);
  });
});
